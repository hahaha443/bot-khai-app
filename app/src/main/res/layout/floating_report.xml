<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <LinearLayout
        android:id="@+id/reportPanel"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@drawable/bg_floating">

        <TextView
            android:id="@+id/dragHandleReport"
            android:layout_width="match_parent"
            android:layout_height="24dp"
            android:background="@drawable/bg_drag_handle_neutral"
            android:gravity="center"
            android:text="≡  Báo cáo giao dịch"
            android:textColor="#FFFFFF"
            android:textSize="11sp" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <LinearLayout
                android:id="@+id/reportList"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingBottom="8dp" />
        </ScrollView>
    </LinearLayout>

    <TextView
        android:id="@+id/resizeHandleReport"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="2dp"
        android:background="@drawable/bg_resize_handle"
        android:gravity="center"
        android:text="⤡"
        android:textColor="#FFFFFF"
        android:textSize="14sp" />

</FrameLayout>
